export const environment = {
  production: true,
  // Mesma origem: API e SPA devem ser servidos pelo mesmo host.
  // Se servir em hosts separados, troque por ex.: 'https://seu-servidor/'
  apiURL: '/'
};
